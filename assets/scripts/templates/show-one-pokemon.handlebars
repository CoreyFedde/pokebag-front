<div id="{{pokemon.name}}SelectorCard" class="pokemon-card">
  <h1>{{pokemon.name}}</h1>
  <h5 id="removePokemon" class="pokemon-selector" data-target='#{{pokemon.name}}Card' data-name='{{pokemon.name}}'>Remove</h5>
</div>
